---
interface Props {
  currentLang: string;
}

const { currentLang } = Astro.props;

const languages = [
  { code: 'es', name: 'Español' },
  { code: 'en', name: 'English' }
];

const currentPath = Astro.url.pathname;
const pathWithoutLang = currentPath.replace(/^\/(en|es)/, '') || '/';
---

<div class="relative">
  <select 
    id="lang-select" 
    aria-label="Select language"
    class="py-2 pr-8 pl-4 border border-slate-300 dark:border-slate-700 rounded-md bg-white dark:bg-slate-800 text-slate-900 dark:text-slate-100 text-sm cursor-pointer appearance-none bg-no-repeat transition-colors duration-200 hover:border-slate-400 dark:hover:border-slate-600 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-0"
    style="background-image: url(&quot;data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' fill='none' viewBox='0 0 20 20'%3e%3cpath stroke='%236b7280' stroke-linecap='round' stroke-linejoin='round' stroke-width='1.5' d='M6 8l4 4 4-4'/%3e%3c/svg%3e&quot;); background-position: right 0.5rem center; background-size: 1.5em 1.5em;"
  >
    {languages.map(lang => (
      <option 
        value={lang.code} 
        selected={currentLang === lang.code}
      >
        {lang.name}
      </option>
    ))}
  </select>
</div>

<script define:vars={{ pathWithoutLang }}>
  const langSelect = document.getElementById('lang-select');
  if (langSelect) {
    langSelect.addEventListener('change', (e) => {
      const newLang = e.target.value;
      window.location.href = `/${newLang}${pathWithoutLang}`;
    });
  }
</script>
